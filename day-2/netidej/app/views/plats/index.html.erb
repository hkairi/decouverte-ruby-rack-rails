<div class="row">
  <div class="col-md-12">
    <%= link_to 'Ajouter un nouveau plat', new_plat_path, class: 'btn btn-sm pull-right' %>
    <table class='table'>
      <thead>
        <tr>
          <th>Nom</th>
          <th colspan="3"> Options</th>
        </tr>
      </thead>

      <tbody>
        <% @plats.each do |plat| %>
          <tr>
            <td><%= plat.nom %></td>
            <td><%= link_to 'Détails', plat %></td>
            <td><%= link_to 'Modifier', edit_plat_path(plat) %></td>
            <td><%= link_to 'Supprimer', plat, method: :delete, data: { confirm: 'Tu blaggues pas ?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
